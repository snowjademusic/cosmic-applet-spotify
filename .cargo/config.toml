# Intentionally left blank for CI compatibility.
#
# If you need vendored/offline builds locally, configure source replacement in a
# user-level Cargo config (for example $CARGO_HOME/config.toml) instead of this
# repository file so release automation can run in clean temporary clones.
